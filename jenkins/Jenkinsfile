pipeline {
    agent none

        stage('Set up Python') {
            agent {
                docker {
                    image 'python' 
                }
            }
            steps {
                sh '''
                    #!/bin/sh
                    cd CodingBuddy
                    python py_compile manage.py
                '''
            }
        }

        stage('Run Migrations') {
            steps {
                sh '''
                    #!/bin/sh
                    cd CodingBuddy
                    python manage.py migrate
                '''
            }
        }

        stage('Run Tests with Coverage') {
            steps {
                sh '''
                    #!/bin/sh
                    cd CodingBuddy
                    coverage run --source='.' manage.py test
                    coverage xml
                '''
            }
        }
    }

    post {
        always {
            junit 'CodingBuddy/reports/*.xml'
        }
    }
}
